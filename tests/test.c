#include<stdio.h>

int main(void)
{
int len;
len = printf("%");
len = printf("%i\n", len);
len = printf("%c");
len = printf("no format %i", len);
void *addr;
addr = (void *)0x7ffe637541f0;
return 0;
}
